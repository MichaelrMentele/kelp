== README

This is Kelp--like Yelp but green and vaguely sea themed.

Download, run bundle install and start your rails server.

You can fully seed the database with:
  - `rake db:migrate`
  - `rake db:seed`
