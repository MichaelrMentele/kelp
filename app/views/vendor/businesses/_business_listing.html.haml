.business.well
  .row
    .col-xs-9
      = link_to business_path(business) do
        %h2
          = "#{(index + 1)} #{business.name}"
        - if current_user.vendor?
          %small
            = link_to "Edit", edit_vendor_business_path(business)
      %small
        = meta(business)
      %p
        = business.blurb
    .col-xs-3
      -# - if business.sale?
      -#   = link_to business_path(business), class: "sale-item text-center" do
      -#     .sale-tag
      -#       Sale! 
      -#     #{business.discount}% Off!